backend = "glx";

blur-method = "dual_kawase";
blur-strength = 2;
blur-background = true;

blur-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'"
];

blur-background-exclude = [
  "class_g = 'slop'", 
  "class_g = 'maim'"
];

